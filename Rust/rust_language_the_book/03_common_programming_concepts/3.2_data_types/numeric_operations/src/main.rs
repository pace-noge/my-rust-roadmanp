
fn main() {
    let sum = 5 + 10;

    // substraction
    let difference = 95.5 - 4.3;

    // multiplication
    let product = 4 * 30;

    // division
    let quotient = 56.7 / 32.2;
    let floored = 2 / 3;


    // remainder
    let remainder = 43 % 5;

    println!("difference {}, product {}, quotient {}, floored {}, remainder {}", difference, product, quotient, floored, remainder);
}
